[
  import_deps: [:ecto_sql, :ecto, :phoenix],
  inputs: [
    "*.{ex,exs}",
    "priv/*/seeds.exs",
    "priv/*/migrations/*.exs",
    "{config,lib,test,apps}/**/*.{ex,exs}",
    "scripts/**/*.{ex,exs}"
  ],
  line_length: 120
]
